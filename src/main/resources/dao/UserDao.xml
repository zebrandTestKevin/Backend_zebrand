<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kevin.zebrand.zebrand_backend.dao.UserDao">
    <select id="getLastInsertId" resultType="java.lang.Integer">
        SELECT LAST_INSERT_ID()
    </select>
    <insert id="save" parameterType="kevin.zebrand.zebrand_backend.model.User">
        INSERT INTO user
        ('user_id','mail','password','name','status')
        VALUES (
                   #{userId},
                   #{mail},
                   #{password},
                   #{status}
               )
    </insert>
</mapper>